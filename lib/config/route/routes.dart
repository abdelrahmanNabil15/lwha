/// Defines the names of the routes used in the application.
class Routes {
  /// The default route for the initial screen (home screen).
  static const String intiRoutes = '/';

  /// The route name for a specific screen (replace 'Your screen' with the actual screen name).
  static const String MainScreen = '/Your screen';
}
